#include <FSM_conf.h>
#include <LRain_FSM.h>
#include <stdint.h>


/**
 * @brief	用户实现，获取当前时间戳
 * @return	当前时间戳
 * @note	用户需要实现该函数，返回当前时间戳，单位由用户指定(推荐为毫秒)
 */
uint32_t fsm_get_tick(void)
{
	//请在这里实现你的时间戳函数
	uint32_t ret = 0;
	return ret;
}
